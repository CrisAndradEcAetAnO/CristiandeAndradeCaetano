#include <stdio.h>
#include <stdlib.h>

int main()
{
  int produto, desconto;
  printf("Informe o preco do produto: ");
  scanf("%d",&produto);

  desconto = 0.1;

  printf("Preco com desconto: %d",desconto);
}
return 0;

